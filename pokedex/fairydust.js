import { fairyDustCursor } from "https://unpkg.com/cursor-effects@latest/dist/esm.js";

// Instantiate the fairyDustCursor once the window has loaded
window.addEventListener("load", () => {
    new fairyDustCursor();
});